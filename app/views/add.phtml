<h1>Add a record</h1>

<p>
<form action="<?php echo Atomik::url('@add'); ?>" method="post">

  <table>
    <tr>
      <td class="right">Artist</td>
      <td class="left">
        <select name="artist_id">
          <?php foreach ($artists as $a): ?>
          <option value="<?php echo $a['id'] ?>"><?php echo $a['name'] ?></option>
          <?php endforeach; ?>
        </select>
      </td>
    </tr>

    <tr>
      <td class="right">Title</td>
      <td class="left"><input name="title" type="text" size="40" /></td>
    </tr>

    <tr>
      <td class="right">Box set</td>
      <td class="left">
        <input name="boxset" type="radio" value="normal" checked="checked" />Normal
        <input name="boxset" type="radio" value="boxset" />Box set
        <input name="boxset" type="radio" value="item" />Part of a box set
      </td>
    </tr>

    <tr>
      <td class="right"></td>
      <td class="left">
        <select name="box_id">
          <?php foreach ($boxsets as $b): ?>
          <option value="<?php echo $b['id'] ?>"><?php echo $b['name'] ?></option>
          <?php endforeach; ?>
        </select>
      </td>
    </tr>

    <tr>
      <td class="right">Type</td>
      <td class="left">
        <select name="type">
          <?php foreach ($types as $t): ?>
          <option><?php echo $t['name'] ?></option>
          <?php endforeach; ?>
        </select>
      </td>
    </tr>

    <tr>
      <td class="right">Year (first release)</td>
      <td class="left"><input name="first_year" type="text" size="40" /></td>
    </tr>

    <tr>
      <td class="right">Year (this release)</td>
      <td class="left"><input name="this_year" type="text" size="40" /></td>
    </tr>

    <tr>
      <td class="right">Format</td>
      <td class="left">
        <select name="format">
          <?php foreach ($formats as $f): ?>
          <option><?php echo $f['name'] ?></option>
          <?php endforeach; ?>
        </select>
      </td>
    </tr>

    <tr>
      <td class="right">Packaging</td>
      <td class="left">
        <select name="packaging">
          <?php foreach ($packagings as $p): ?>
          <option><?php echo $p['name'] ?></option>
          <?php endforeach; ?>
        </select>
      </td>
    </tr>

    <tr>
      <td class="right">Label</td>
      <td class="left">
        <select name="label_id">
          <?php foreach ($labels as $l): ?>
          <option value="<?php echo $l['id'] ?>"><?php echo $l['name'] ?></option>
          <?php endforeach; ?>
        </select>
      </td>
    </tr>

    <tr>
      <td class="right">Limited</td>
      <td class="left">
        <input name="limited" type="checkbox" value="limited">
        <input name="ltd_num" type="text" size="10" />
      </td>
    </tr>

    <tr>
      <td class="right">Added</td>
      <td class="left">
        <input name="added" type="text" size="20" /> (yyyy-mm-dd)
      </td>
    </tr>

    <tr>
      <td class="right">Lent</td>
      <td class="left">
        <input name="lent" type="checkbox" value="lent"> to
        <input name="borrower" type="text" size="30" />
      </td>
    </tr>

    <tr>
      <td class="right">Annotation</td>
      <td class="left">
        <textarea name="annotation" rows="4" cols="38"></textarea>
      </td>
    </tr>

  </table>

  <br />
  <input class="button" name="submit" type="submit" value="Save" />
  <input class="button" name="cancel" type="reset" value="Cancel" />

</form>
</p>
